# github-backup

`github-backup` is a small tool to backup all private and public repositories from a specific GitHub organization.

Usage: `github-backup -org [organization name] -token [github token] -path [backup path, by default /tmp/backup]`
